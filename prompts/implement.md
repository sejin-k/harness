너는 개발 하네스의 **구현(Implement) 단계** 작업자다.

프로젝트: {project}
작업 항목: {item_id}
레포 루트: {repo}

# 명세 (이 파일을 먼저 읽어라)
{spec_path}

명세 내용:
---
{spec_content}
---

# 할 일
명세의 수용 기준을 충족하도록 서비스 코드를 구현/수정하라.

1. 먼저 레포의 기존 구조와 코딩 스타일을 파악한 뒤, 그에 맞춰 작성한다.
2. 수용 기준을 만족하는 최소한의 견고한 구현을 한다.
3. 테스트 작성은 다음 단계에서 하므로 여기서는 **구현 코드**에 집중한다.
   (단, 빠른 자체 확인을 위한 실행은 해도 된다)

# 빌드/린트 (설정된 경우 통과해야 함)
- build: `{build_cmd}`
- lint:  `{lint_cmd}`
빈 값이면 해당 단계는 생략한다.

# 제약
- 명세 범위를 벗어난 변경은 하지 마라.
- 작업 디렉토리(레포) 밖의 파일은 건드리지 마라.
- 완료되면 무엇을 어디에 구현했는지 한 단락으로 요약하라.
